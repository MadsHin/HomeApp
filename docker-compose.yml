services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: homeapp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - homeapp_db:/var/lib/postgresql/data

volumes:
  homeapp_db:
